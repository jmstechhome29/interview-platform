apiVersion: v2
name: api-gateway
description: api-gateway (thin chart — all real templating lives in the shared "common" library chart)
version: 1.0.0
dependencies:
  - name: common
    version: 1.0.0
    repository: "file://../../common"
